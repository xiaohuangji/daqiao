package com.renren.intl.soundsns.rmi.test;

import java.util.List;

import org.junit.Ignore;
import org.junit.Test;

import com.renren.intl.soundsns.model.News;
import com.renren.intl.soundsns.rmi.client.NewsServiceDelegate;

import junit.framework.TestCase;

public class MessageTestCase {
	final NewsServiceDelegate newsDelegate = new NewsServiceDelegate();

	@Ignore @Test
	public void testSend() {
		News news = new News();
		news.setBody("test @");
		news.setFromId(10000213);
		news.setFeedOwnerId(10000213);
		news.setIsRead(0);
		news.setSourceId(2);
		news.setCreateTime(System.currentTimeMillis());
		news.setType(0);
		news.setUserId(10000215);
		newsDelegate.post(news);
	}

	@Ignore @Test
	public void testGetNewsList() throws Exception {
		List<News> news = newsDelegate.getNewsList(10000215, 10);
		if (news == null) {
			throw new Exception();
		}
	}
}
